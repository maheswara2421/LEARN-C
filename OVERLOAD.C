/*prg to overload the binary operater plus*/
#include<iostream.h>
#include<conio.h>
#include<string.h>
const int max=100
class string
     {
       private;
	  char st[max];
       public:
	  string();
     {
	st[10]='\0';
     }
       string(char*s);
     {
	syrepy(st,s);
     }
       cout<<"the string is "<<endl
     }
       string operater*(string s2);
     {
      if